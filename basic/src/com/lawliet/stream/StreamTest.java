package com.lawliet.stream;

/**
 * @author lawliet.L
 */
public class StreamTest {

    public static void main(String[] args) {
//        Random random = new Random();
//        random.ints().limit(10).forEach(System.out::println);

//        List<Integer> list = Arrays.asList(1, 2, 2, 4, 5, 6);
//        List<Integer> collect = list.stream().map(num -> num * num).collect(Collectors.toList());     // 输出 [1, 4, 4, 16, 25, 36]
//        List<Integer> collect = list.stream().map(num -> num * num).distinct().collect(Collectors.toList());    // 输出 [1, 4, 16, 25, 36]
//        System.out.println(collect);

//        List<Integer> list = Arrays.asList(1, 2, 2, 4, 5, 6);
//        list.stream().filter(num -> num > 3).forEach(System.out::println);

//        List<Integer> list = Arrays.asList(1, 2, 2, 4, 5, 6);
//        list.stream().filter(num -> num > 3).limit(2).forEach(System.out::println);

//        List<Integer> list = Arrays.asList(1, 2, 2, 4, 5, 6);
//        list.stream().filter(num -> num > 3).collect(Collectors.toList()).forEach(System.out::println);

//        List<Integer> list = Arrays.asList(1, 2, 2, 4, 5, 6);
//        IntSummaryStatistics statistics = list.stream().mapToInt((x) -> x).summaryStatistics();
//        System.out.println(statistics.getAverage());
//        System.out.println(statistics.getMax());
//        System.out.println(statistics.getCount());
//        System.out.println(statistics.getMin());
//        System.out.println(statistics.getSum());

//        List<String> list = Arrays.asList("hello world", "hello java", "hello every one");
//        list.stream().map(v -> v.split(" ")).peek(System.out::println).count();
//        list.stream().flatMap(v -> Arrays.stream(v.split(" "))).peek(System.out::println).count();

    }
}
